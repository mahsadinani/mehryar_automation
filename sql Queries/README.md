# 🗄️ راهنمای اجرای اسکریپت‌های SQL

این پوشه شامل اسکریپت‌های SQL برای راه‌اندازی دیتابیس Supabase هست.

## 📁 فایل‌ها

- **00_reset.sql** - پاک کردن تمام جدول‌ها
- **01_schema.sql** - ساخت جدول‌ها و ایندکس‌ها
- **02_seed_school_courses.sql** - اضافه کردن 74 دوره مدرسه
- **03_seed_tech_courses.sql** - اضافه کردن 1 دوره فنی (ICDL)
- **seed-database.js** - اسکریپت اتوماتیک برای اجرای تمام فایل‌ها

## 🚀 نحوه استفاده

### روش 1: اجرای دستی (توصیه شده)
1. به داشبورد Supabase برید
2. به بخش **SQL Editor** برید
3. فایل‌ها رو به ترتیب اجرا کنید:
   - اول `00_reset.sql`
   - بعد `01_schema.sql`
   - بعد `02_seed_school_courses.sql`
   - در آخر `03_seed_tech_courses.sql`

### روش 2: استفاده از اسکریپت اتوماتیک

1. مطمئن شید که ENV variables تنظیم شدن:
   ```bash
   SUPABASE_URL=your_supabase_url
   SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
   ```

2. اسکریپت رو اجرا کنید:
   ```bash
   cd "sql Queries"
   node seed-database.js
   ```

## ✅ بعد از اجرا

بعد از اینکه اسکریپت‌ها رو اجرا کردید:

1. به آدرس Vercel برید
2. برید به صفحه اصلی برنامه
3. باید دوره‌ها رو ببینید!

## 🔍 تست کردن

برای تست کردن API:
```bash
# مشاهده تمام دوره‌ها
curl https://your-app.vercel.app/api/courses

# مشاهده دوره‌های فنی
curl https://your-app.vercel.app/api/tech-courses
```

## 📞 در صورت مشکل

اگر ارور داشتید:
1. لاگ‌های Vercel رو چک کنید
2. مطمئن شید ENV variables درست تنظیم شدن
3. مطمئن شید فایل‌های SQL بدون خطا اجرا شدن